language: python
python:
    - "2.7"
    - "3.3"
    - "3.4"
    - "3.5"
    - "3.6"
sudo: false
env:
  global:
    LD_PRELOAD=/lib/x86_64-linux-gnu/libSegFault.so
  matrix:
    - TOXENV=check
    - TOXENV=np{1.8,1.9,1.10,1.14},extension-coveralls,coveralls
    - TOXENV=np{1.8,1.9,1.10,1.14}-nocover
before_install:
  - python --version
  - uname -a
  - lsb_release -a
install:
  - pip install tox
  - virtualenv --version
  - easy_install --version
  - pip --version
  - tox --version
script:
  - tox -v
after_failure:
  - more .tox/log/* | cat
  - more .tox/*/log/* | cat
notifications:
  email:
    on_success: never
    on_failure: always
matrix:
  allow_failures:
    - env: "TOXENV=check"
    - python: 3.3
      env: "TOXENV=np1.14-nocover"
    - python: 3.3
      env: "TOXENV=np1.14,extension-coveralls,coveralls"